suites:
  piddling_domain:
    namespace: Piddling
    src_path: src
    spec_path: tests
    spec_prefix: spec
  piddling_framework:
    namespace: Framework
    src_path: src
    spec_path: tests
    spec_prefix: spec
extensions:
    - PhpSpec\Extension\CodeCoverageExtension

code_coverage:
  format: clover
  output: coverage.xml